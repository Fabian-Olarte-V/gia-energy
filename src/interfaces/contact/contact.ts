export interface ServiceOption {
        value: string;
        label: string;
}

export interface FormData {
    selectedService: string;
    name: string;
    city: string;
    phone: string;
    email: string;
}